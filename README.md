# TASK4
Create a class EthPacket with a dynamic array payload. 2. Add a variable
len (length). 3. Constraint: payload.size() must equal len. 4. Constraint: len
must be between 4 and 8. 5. Randomize and print 5 packets to prove the payload size
changes correctly
